# Leave unset for local dev — PGlite runs embedded and persists to .data/
# Set to a Neon connection string for production (Vercel)
DATABASE_URL=

# Used by the orchestrator in later blocks (optional; fallback works without it)
ANTHROPIC_API_KEY=
